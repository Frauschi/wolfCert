# Generated by autoconf; do not edit. Defines the imported target that the
# native CMake build exports via install(EXPORT), so consumers see the same
# wolfCert::wolfcert target regardless of which build system installed wolfCert.

if(NOT TARGET wolfCert::wolfcert)
  add_library(wolfCert::wolfcert UNKNOWN IMPORTED)

  set(_wolfcert_libdir "@WOLFCERT_LIBDIR_ABS@")
  set(_wolfcert_includedir "@WOLFCERT_INCLUDEDIR_ABS@")

  find_library(WOLFCERT_LIBRARY NAMES wolfcert PATHS "${_wolfcert_libdir}" NO_DEFAULT_PATH)
  if(NOT WOLFCERT_LIBRARY)
    find_library(WOLFCERT_LIBRARY NAMES wolfcert)
  endif()
  if(NOT WOLFCERT_LIBRARY)
    message(FATAL_ERROR "wolfcert library not found. Looked in: ${_wolfcert_libdir}")
  endif()

  set_target_properties(wolfCert::wolfcert PROPERTIES
    IMPORTED_LOCATION "${WOLFCERT_LIBRARY}"
    INTERFACE_INCLUDE_DIRECTORIES "${_wolfcert_includedir}"
    INTERFACE_LINK_LIBRARIES wolfssl::wolfssl
  )
endif()
